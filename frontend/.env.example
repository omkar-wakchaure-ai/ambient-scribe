# Copy this file to `.env` (or `.env.local`) inside the frontend/ directory
# and uncomment/adjust the values below.

# Base URL of the FastAPI backend. The Vite dev server proxies /api to this
# host, so this only needs adjusting if you are NOT using the proxy.
VITE_API_BASE_URL=http://127.0.0.1:8000