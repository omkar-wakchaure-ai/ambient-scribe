# Logs, caches, model artifacts (Person A)
*.log
__pycache__/
*.py[cod]
sample_data/

# Environment / secrets - never commit real API keys (Person B)
.env
.env.local
.env.*
!.env.example

# Virtual environments
venv/
.venv/

# Runtime uploads (FastAPI)
app/storage/uploads/